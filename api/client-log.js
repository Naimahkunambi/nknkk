export default async function handler(req,res){if(req.method!=='POST')return res.status(405).json({error:'Method not allowed'});try{const b=req.body||{};console.error('[HARVEST CLIENT]',JSON.stringify({type:String(b.type||'client'),stage:String(b.stage||''),message:String(b.message||''),at:String(b.at||new Date().toISOString())}));return res.status(200).json({ok:true})}catch(error){return res.status(500).json({error:error.message||'Could not record client diagnostic'})}}